export interface User {
    id: number;
    name: string;
    email: string;
    month: string;
    monthIndex: number;
    // eslint-disable-next-line
    date: any;
    // eslint-disable-next-line
    BirthdayDate: any;
  }   